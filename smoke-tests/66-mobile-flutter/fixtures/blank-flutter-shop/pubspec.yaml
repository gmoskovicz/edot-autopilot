name: shop_app
description: ShopApp Flutter — no OTel (fixture for 66-mobile-flutter eval)
version: 4.2.1+841

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
